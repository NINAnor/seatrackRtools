# Common test setup - sourced by all test files
# This file ensures consistent test configuration and helper availability

# Source test helpers
source(file.path(dirname(test_path()), "test_helpers.R"), local = TRUE)
