# Run the Shiny App

Run the Shiny App

## Usage

``` r
run_app(
  settings_path = file.path(getwd(), "seatrackRtools_app"),
  log_path = file.path(getwd(), "seatrackRtools_app", "logs"),
  test = FALSE
)
```
