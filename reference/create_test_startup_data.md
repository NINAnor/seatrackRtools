# Create test startup data Generates a test startup sheet as a tibble for use in tests.

Create test startup data Generates a test startup sheet as a tibble for
use in tests.

## Usage

``` r
create_test_startup_data(n_rows = 5)
```

## Value

A tibble representing a test startup sheet.
