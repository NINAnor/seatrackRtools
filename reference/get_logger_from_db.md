# Find logger instances in database

This function tries to find a logger ID in the database. It returns a
data frame with the logger information.

## Usage

``` r
get_logger_from_db(logger_id)
```

## Arguments

- logger_id:

  logger ID of desired logger

## Value

data frame with logger information
